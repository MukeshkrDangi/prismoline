import GalleryClient from "./GalleryClient";

export const metadata = {
  title: "Gallery — Prismoline",
  description: "Events, Seminars, Awards & Exhibitions gallery.",
};

export default function GalleryPage() {
  return <GalleryClient />;
}
